package schema;

public class FullSchema {
    Schema schema;
    SchemaShape shape;

    public FullSchema(Schema schema, SchemaShape shape){
        this.schema = schema;
        this.shape = shape;
    }
}
